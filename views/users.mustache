<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>

    </head>
    <body>
        <div class="users">
            <div class="show2">
            <img class="picture"src="{{users.avatar}}">
            <h2> {{users.name}}</h2>
            <h3> {{users.address.city}} , {{users.address.country}} |{{users.company}}, {{#job}}{{.}}{{/job}}
            {{^job}} Availale to Hire {{/job}} </h3>
            <h3> {{users.email}} | {{users.phone}} </h3>
            <h4>Eductaion : {{users.university}}</h4>
            <h4>Skill : {{users.skills}}</h4>
          </div>

        </div>

    </body>
</html>
